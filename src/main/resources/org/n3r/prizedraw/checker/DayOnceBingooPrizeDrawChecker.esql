[checkBingooTodayRecord]
SELECT 1
FROM PRIZE_BINGOO
WHERE ACTIVITY_ID = ##
AND   USER_ID = ##
AND   ITEM_JOIN = 0
AND   BINGOO_TIME BETWEEN TRUNC(SYSDATE, 'DD') AND TRUNC(SYSDATE + 1, 'DD')
AND   ROWNUM < 2
